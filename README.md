1. Install dependencies  `npm install`
2. Set Environtment Variable "Hubungi Developer karena ini rahasia dan tidak boleh di publish sembarangan"
3. Setelah melakukan    `npm install`   maka bisa langsung di running  `npm run dev`
